{
  "name": "zkp-circuits",
  "version": "1.0.0",
  "description": "ZKP circuits for career passport using circom and ZKPlonK",
  "scripts": {
    "compile": "node scripts/compile.js",
    "setup": "node scripts/setup.js",
    "test": "node scripts/test.js",
    "build:all": "npm run compile && npm run setup"
  },
  "dependencies": {
    "circomlib": "^2.0.5",
    "circom_runtime": "^0.1.0"
  },
  "devDependencies": {
    "circom": "^0.5.46",
    "snarkjs": "^0.7.5"
  }
}
